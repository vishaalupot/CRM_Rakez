@using Microsoft.AspNet.Identity
<!DOCTYPE html>
<html>
<head>
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <style>
                @@import url('https://fonts.googleapis.com/css2?family=Jaldi&display=swap');

        .navbar {
            /*            background-color: #082948;
        */
            background: linear-gradient(to right,#082948 15%, #00806e 95%);
            height: 80px;
            box-shadow: rgba(14, 30, 37, 0.12) 0px 2px 4px 0px, rgba(14, 30, 37, 0.32) 0px 2px 16px 0px;
        }

                .font {
                    color: white;
                }

                .logo1 {
                    position: sticky;
                    left: 0;
                }
    </style>

    <style>




        .btns {
            font-size: 15px;
            background: transparent;
            border: none;
            padding: 0.5em 1em;
            color: white;
            text-transform: uppercase;
            position: relative;
            transition: 0.5s ease;
            cursor: pointer;
            font-weight: 500;
        }

            .btns::before {
                content: "";
                position: absolute;
                left: 0;
                bottom: 0;
                height: 2px;
                width: 0;
                background-color: white;
                transition: 0.5s ease;
                color: white;
            }

            .btns:hover {
                color: white;
                transition-delay: 0.5s;
            }

                .btns:hover::before {
                    width: 100%;
                    color: white;
                }

            .btns::after {
                content: "";
                position: absolute;
                left: 0;
                bottom: 0;
                height: 0;
                width: 100%;
                background-color: white;
                transition: 0.4s ease;
                z-index: -1;
                color: white;
            }

            .btns:hover::after {
                height: 100%;
                transition-delay: 0.4s;
                color: white;
            }

        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 1;
            width: 140px;
        }

            .dropdown-content a {
                color: black;
                padding: 12px 16px;
                text-decoration: none;
                display: block;
                width: 140px;
            }

        .dropdown:hover .dropdown-content {
            display: block;
        }

        .dropdown-content a {
            width: 140px;
            border-radius: 4px;
            white-space: nowrap;
        }

            .dropdown-content a:hover {
                background-color: #082948;
                color: white;
                font-weight: 500;
                width: 140px;
            }



        .form {
            --input-bg: #FFf;
            /*  background of input */
            --padding: 1.5em;
            --rotate: 80deg;
            /*  rotation degree of input*/
            --gap: 2em;
            /*  gap of items in input */
            --icon-change-color: #15A986;
            /*  when rotating changed icon color */
            --height: 32px;
            /*  height */
            width: 400px;
            padding-inline-end: 0em;
            /*  change this for padding in the end of input */
            background: var(--input-bg);
            position: relative;
            border-radius: 6px;
        }

            .form label {
                border: 1px solid #818181;
                display: flex;
                align-items: center;
                width: 100%;
                height: var(--height);
                border-radius: 6px;
            }

            .form input {
                width: 100%;
                padding-inline-start: calc(var(--padding) + var(--gap));
                outline: none;
                background: none;
                border: 0;
            }
            /* style for both icons -- search,close */
            .form svg {
                /* display: block; */
                color: #111;
                transition: 0.3s cubic-bezier(.4,0,.2,1);
                position: absolute;
                height: 15px;
            }
        /* search icon */
        .icon {
            position: absolute;
            left: var(--padding);
            transition: 0.3s cubic-bezier(.4,0,.2,1);
            display: flex;
            justify-content: center;
            align-items: center;
        }
        /* arrow-icon*/
        .swap-off {
            transform: rotate(-80deg);
            opacity: 0;
            visibility: hidden;
        }
        /* close button */
        .close-btn {
            /* removing default bg of button */
            background: none;
            border: none;
            right: calc(var(--padding) - var(--gap));
            box-sizing: border-box;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #111;
            padding: 0.1em;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            transition: 0.3s;
            opacity: 0;
            transform: scale(0);
            visibility: hidden;
        }

        .form input:focus ~ .icon {
            transform: rotate(var(--rotate)) scale(1.3);
        }

            .form input:focus ~ .icon .swap-off {
                opacity: 1;
                transform: rotate(-80deg);
                visibility: visible;
                color: var(--icon-change-color);
            }

            .form input:focus ~ .icon .swap-on {
                opacity: 0;
                visibility: visible;
            }

        .form input:valid ~ .icon {
            transform: scale(1.3) rotate(var(--rotate))
        }

            .form input:valid ~ .icon .swap-off {
                opacity: 1;
                visibility: visible;
                color: var(--icon-change-color);
            }

            .form input:valid ~ .icon .swap-on {
                opacity: 0;
                visibility: visible;
            }

        .form input:valid ~ .close-btn {
            opacity: 1;
            visibility: visible;
            transform: scale(1);
            transition: 0s;
        }

        .position-relative {
    position: relative;
}

.badge {
    position: absolute;
    top: -5px;
    right: -5px;
    padding: 4px 8px;
    border-radius: 12px;
    background-color: red;
    color: white;
    font-size: 0.75rem;
    font-weight: bold;
    line-height: 1;
    text-align: center;
    min-width: 20px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.15);
    transition: transform 0.3s ease-in-out;
}

.badge-change {
    animation: badgeChange 0.5s ease-in-out;
}

@@keyframes badgeChange {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.2); }
}


    </style>

</head>

@*<li>@Html.ActionLink("Home", "Index", "Home", new { area = "" }, new { @class = "nav-link font" })</li>
    <li>@Html.ActionLink("About", "About", "Home", new { area = "" }, new { @class = "nav-link font" })</li>
    <li>@Html.ActionLink("Contact", "Contact", "Home", new { area = "" }, new { @class = "nav-link font" })</li>*@
<body>
    <nav class="navbar navbar-expand-sm navbar-toggleable-sm">
        <div class="container">
            <div class="collapse navbar-collapse d-sm-inline-flex justify-content-between">
                <a href="@Url.Action("Index", "Home")">
                    <div class="logo1">
                        @*<img src="~/Content/Images/logo1.png" style="height: 45px;" class="img-fluid align-items-center" />*@
                        <img src="~/Content/Images/derbywhi.png" style="height: 30px; padding-bottom: 2px;" class="img-fluid align-items-center" />
                        <img src="~/Content/Images/line.png" style="height: 28px;" class="img-fluid align-items-center" />
                    </div>
                </a>


                <ul class="navbar-nav flex-grow-1">
                    <li>

                        <button class="btns position-relative" onclick="redirectToHome()">
                            Home
                            </button>



                    </li>
                    <li class="dropdown">
                        <button class="btns">Reports</button>
                        <div class="dropdown-content">
                            <a href="@Url.Action("CRMPPF", "Home")">CRM PPF</a>
                            <a href="@Url.Action("MasterReport", "Home")">Master Data</a>
                            <a href="@Url.Action("AllocationReport", "Home")">Allocation Data</a>
                        </div>
                    </li>

                    @if (!(User.IsInRole("Agent")))
                    {
                        <li><button class="btns" onclick="redirectToUpload()">Upload Cases</button></li>
                    }
                    <li><button class="btns" onclick="redirectToEdit()">Edit Allocation</button></li>
                    
                </ul>

                @Html.Partial("_LoginPartial")
            </div>
        </div>

    </nav>
    <div class="container body-content ">
        @RenderBody()

    </div>

    <script>
         function redirectToEdit() {
             window.location.href = '@Url.Action("EditAllocation", "Home")';
        }

        function redirectToHome() {
             window.location.href = '@Url.Action("Index", "Home")';
        }

         function redirectToUpload() {
             window.location.href = '@Url.Action("UploadCases", "Home")';
        }


    </script>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
